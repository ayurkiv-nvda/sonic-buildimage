# DPKG FRK

SPATH := $($(SONIC_SRC_FILES)_SRC_PATH)
DEP_FILES := $(SONIC_COMMON_FILES_LIST) rules/sonic-src.mk rules/sonic-src.dep
DEP_FILES += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES += $(shell git ls-files -- $(SPATH))

$(SONIC_SRC_FILES)_CACHE_MODE := GIT_CONTENT_SHA
$(SONIC_SRC_FILES)_DEP_FLAGS := $(SONIC_COMMON_FLAGS_LIST)
$(SONIC_SRC_FILES)_DEP_FILES := $(DEP_FILES)

